name: Update Images Timestamp

on:
  schedule:
    - cron: "0 */2 * * *"  # 每2小时运行一次
  workflow_dispatch:        # 手动触发

permissions:
  contents: write

jobs:
  update-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史，确保git diff能正常工作

      - name: Update README with timestamp
        run: |
          # 生成时间戳
          TIMESTAMP=$(date +%s)
          
          # 备份原文件
          cp README.md README.md.bak
          
          # 更新Star History图片 - 使用更精确的锚点匹配
          sed -i "/## 📈 仓库 Star 历史/,/^---/s|\(https://api.star-history.com/svg?repos=ColorFulCraft/CFCHistory&type=date&legend=top-left\)\([^)]*\)|\1\&v=$TIMESTAMP|" README.md
          
          # 更新Repobeats图片 - 使用锚点匹配
          sed -i "/## 🗓️ 最近提交/,/^---/s|\(https://repobeats.axiom.co/api/embed/a30781bf5e8a72396572aed91c5588cc9e496ca0\.svg\)\([^)]*\)|\1?v=$TIMESTAMP|" README.md
          
          # 检查是否有更改
          if diff -q README.md README.md.bak >/dev/null 2>&1; then
            echo "No changes needed"
          else
            echo "README images updated with new timestamp: $TIMESTAMP"
          fi
          
          # 清理备份
          rm README.md.bak

      - name: Commit and push changes
        if: success()
        run: |
          git config user.name "CFCAT"
          git config user.email "leng@ipacel.cc"
          
          # 检查文件是否有实际变化
          if git diff --quiet README.md; then
            echo "No changes to commit"
            exit 0
          fi
          
          git add README.md
          git commit -m "CFCAT: 😊我来更新图片的时间戳啦qwq~ [自动更新]"
          git push
