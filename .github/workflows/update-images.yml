name: Update Images Timestamp

on:
  schedule:
    - cron: "0 */2 * * *"  # 每2小时运行一次
  workflow_dispatch:        # 手动触发

permissions:
  contents: write

jobs:
  update-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update README with timestamp
        run: |
          # 生成时间戳
          TIMESTAMP=$(date +%s)
          
          # 创建临时文件
          cp README.md README.md.tmp
          
          # 修复已有的链接（先去掉可能不完整的部分）
          sed -i 's|star-history.svg?v=[0-9]*$|star-history.svg|g' README.md.tmp
          sed -i 's|repobeats.svg?v=[0-9]*$|repobeats.svg|g' README.md.tmp
          
          # 使用更精确的匹配方式
          # 匹配星历史图片：![Star历史](URL)
          sed -i "s|\(!\[Star历史\](https://raw\.githubusercontent\.com/ColorFulCraft/CFCHistory/main/pictures/star-history\.svg\)\(?v=[0-9]*\)\?|\\1?v=$TIMESTAMP|g" README.md.tmp
          
          # 匹配代码活动图片：![代码活动](URL)
          sed -i "s|\(!\[代码活动\](https://raw\.githubusercontent\.com/ColorFulCraft/CFCHistory/main/pictures/repobeats\.svg\)\(?v=[0-9]*\)\?|\\1?v=$TIMESTAMP|g" README.md.tmp
          
          # 另一种方法：直接替换整个图片标记
          sed -i "s|!\[Star历史\](https://raw\.githubusercontent\.com/ColorFulCraft/CFCHistory/main/pictures/star-history\.svg[^)]*)|![Star历史](https://raw.githubusercontent.com/ColorFulCraft/CFCHistory/main/pictures/star-history.svg?v=$TIMESTAMP)|g" README.md.tmp
          
          sed -i "s|!\[代码活动\](https://raw\.githubusercontent\.com/ColorFulCraft/CFCHistory/main/pictures/repobeats\.svg[^)]*)|![代码活动](https://raw.githubusercontent.com/ColorFulCraft/CFCHistory/main/pictures/repobeats.svg?v=$TIMESTAMP)|g" README.md.tmp
          
          # 检查是否有更改
          if ! cmp -s README.md README.md.tmp; then
            mv README.md.tmp README.md
            echo "README images updated with new timestamp: $TIMESTAMP"
          else
            echo "No changes needed"
            rm README.md.tmp
          fi

      - name: Commit and push changes
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 更新图片时间戳qwq~ [skip ci]" && git push)
